<div class="new-booking">
  <% if flash[:alert] %>
    <div id="alert">
      <%= flash[:alert] %> 
    </div>
  <% end %>
  <%= render 'new_booking_form' %>
  <button><%= link_to 'Restart Flight Search', root_path, :data => {:confirm => "Are you sure?"} %>
